import React, { useState } from "react";
import {
  BarChart,
  Bar,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  Legend,
  ResponsiveContainer,
} from "recharts";

const Test = () => {

  return (

    <input
      type="value"
      name="date"
      value="500"
      //onChange={handleChange}
      className="border rounded-lg p-2"
    />
  );
};

export default Test;
